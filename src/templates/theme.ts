export default interface ThemeInterface {
  primaryColor: string;
  secondaryColor: string;
  bgColor: string;
  tags: {
    yellow: string;
    black: string;
    red: string;
    green: string;
  };
}
