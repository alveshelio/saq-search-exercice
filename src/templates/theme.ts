export default interface ThemeInterface {
  primary: string;
  reversedPrimary: string;
  secondary: string;
  bgColor: string;
  tags: {
    yellow: string;
    black: string;
    red: string;
    green: string;
  };
}
